# Pre-commit hooks for Java projects
# Install: pip install pre-commit  →  pre-commit install
# Run manually: pre-commit run --all-files
# Docs: https://pre-commit.com

repos:

  # ---------------------------------------------------------------------------
  # General hygiene
  # ---------------------------------------------------------------------------
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: trailing-whitespace
        args: [--markdown-linebreak-ext=md]
      - id: end-of-file-fixer
      - id: mixed-line-ending
        args: [--fix=lf]
      - id: check-xml                   # validates build files (pom.xml, build.gradle.kts, etc.)
      - id: check-yaml
        args: [--unsafe]
      - id: check-json
      - id: check-added-large-files
        args: [--maxkb=1024]
      - id: detect-private-key
      - id: check-merge-conflict
      - id: no-commit-to-branch
        args: [--branch=main, --branch=master]

  # ---------------------------------------------------------------------------
  # Secret detection
  # ---------------------------------------------------------------------------
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.5.0
    hooks:
      - id: detect-secrets
        args: [--baseline, .secrets.baseline]

  # ---------------------------------------------------------------------------
  # Add your build-tool-specific hooks below.
  # Examples:
  #
  # Maven Checkstyle:
  #   - repo: local
  #     hooks:
  #       - id: checkstyle
  #         name: Maven Checkstyle
  #         language: system
  #         entry: bash -c 'chmod +x mvnw && ./mvnw checkstyle:check -q'
  #         pass_filenames: false
  #         files: \.java$
  #
  # Gradle check:
  #   - repo: local
  #     hooks:
  #       - id: gradle-check
  #         name: Gradle check
  #         language: system
  #         entry: bash -c 'chmod +x gradlew && ./gradlew check -q'
  #         pass_filenames: false
  #         files: \.java$
  # ---------------------------------------------------------------------------
