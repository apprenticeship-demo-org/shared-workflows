{
  "recommendations": [
    // YAML: schema validation and IntelliSense
    "redhat.vscode-yaml",
    // Terraform: HCL syntax, validation, formatting
    "HashiCorp.terraform",
    // Docker: Dockerfile syntax and compose support
    "ms-azuretools.vscode-docker",
    // SonarLint: flags issues inline before you push
    "SonarSource.sonarlint-vscode",
    // Hadolint: Dockerfile linting inline
    "exiasr.hadolint",
    // Helm: chart syntax and completion
    "Tim-Koehler.helm-intellisense"
  ]
}
