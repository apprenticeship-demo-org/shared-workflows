name: CI

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.vscode/**'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/PULL_REQUEST_TEMPLATE.md'
      - '.trivyignore'
      - '.gitignore'
  pull_request:
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.vscode/**'
      - '.github/ISSUE_TEMPLATE/**'
      - '.github/PULL_REQUEST_TEMPLATE.md'
      - '.trivyignore'
      - '.gitignore'

# Permissions required by the shared-workflows pipeline chain.
# security-events: write is needed for Trivy SARIF upload to the Security tab.
permissions:
  contents: read
  security-events: write

jobs:
  pipeline:
    # ── Replace <YOUR-ORG> with your GitHub organisation name ──────────────
    uses: <YOUR-ORG>/shared-workflows/.github/workflows/pipeline.yml@main
    secrets: inherit
    # Optional overrides — uncomment and edit only the lines you need:
    # with:
    #   terraform-version: "latest"   # default: latest
    #   dockerfile-path: "Dockerfile" # default: Dockerfile
    #   sonar-project-key: ""         # default: auto-derived as {org}_{repo}
