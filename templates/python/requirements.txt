# Project runtime dependencies — add your own below.
#
# Dev/test tooling (pytest, pytest-cov, flake8, requests) is pre-installed
# by the CI pipeline and does NOT need to be listed here.
#
# Examples:
#   flask>=3.0.0
#   sqlalchemy>=2.0.0
#   pydantic>=2.0.0
